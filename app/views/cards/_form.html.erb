<%= form_with model: [@deck, @card], local: true do |form| %>
    <% if @card.errors.any? %>
        <div id="error_explanation">
            <h2>
                <%= pluralize(@card.errors.count, "error") %> prohibited this article from being saved:
            </h2>
            <ul>
                <% @card.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
            </ul>
        </div>
    <% end %>
    <p>
        <%= form.label :front %><br>
        <%= form.text_field :front %>
    </p>
    <p>
        <%= form.label :verse %><br>
        <%= form.text_area :verse %>
    </p>
    <p>
        <%= form.submit %>
    </p>
<% end %>
